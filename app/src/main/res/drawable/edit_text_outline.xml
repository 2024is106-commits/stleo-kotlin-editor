<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Main Outline -->
    <item>
        <shape>
            <solid android:color="@android:color/transparent" />
            <stroke android:width="1dp" android:color="@color/text_off_white" />
        </shape>
    </item>

    <!-- Corner Accents (Manual simulation of the small L-shapes in the UI guide) -->
    <!-- We'll just use a clean outline for now to maintain the Silkscreen aesthetic -->
</layer-list>
